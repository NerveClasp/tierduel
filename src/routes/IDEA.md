the idea is to make the following changes: make it so that the home page is a list of tierlists with a button to create a new one at the top. so that instead of one tierlist users would be able to have multiple, and they were able to rename them. To make these persistent, I'd like you to implement reading/writing to local storage, probably writing a wrapper for stores would do the trick or something. For example we have a store with hardcoded id `list` which is an array of tierlists ids, and each store for the tierlist has that id and a name and the rest of tierlist data or something. Or maybe just have a one store. But I do want to have autosave to local storage for sure. Please first create a PLAN.md and write a more articulate version of this message of mine so that you can read it in case something happens to this chat or there some glitch in the system that will prevent you from finishing the task please

